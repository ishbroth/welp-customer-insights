# Supabase Configuration (PUBLIC - Safe to commit)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Stripe Configuration (PUBLIC - Safe to commit)
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_your-publishable-key

# App Configuration
VITE_APP_ENV=development
VITE_LOG_LEVEL=debug

# ============================================================
# IMPORTANT: DO NOT PUT SECRET KEYS IN THIS FILE
# ============================================================
# The following secrets should ONLY be stored in:
# - Supabase Edge Functions secrets (for production)
# - Local development environment variables
#
# Secret keys to configure elsewhere:
# - STRIPE_SECRET_KEY (Supabase Edge Functions)
# - RESEND_API_KEY (Supabase Edge Functions)
# - FCM_SERVER_KEY (Supabase Edge Functions)
# - APNS_KEY_ID (Supabase Edge Functions)
# - APNS_TEAM_ID (Supabase Edge Functions)
# - APNS_KEY (Supabase Edge Functions)
#
# NEVER commit files containing these secret keys to Git!
# ============================================================
